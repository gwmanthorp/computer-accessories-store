
public class Admin extends User{

	public Admin(int userID, String username, String name, Address address, UserRole role) {
		super(userID, username, name, address, role); // Call the superclass constructor (User constructor).
		
	}

}
